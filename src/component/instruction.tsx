import React from 'react';

export default function Instruction(){
    return <div className="Instruction">
        <p>This site is simple application for rolling genesys dices. To generate random numbers, it’s use <a href="https://www.random.org/" target="_blank" rel="noopener noreferrer">random.org page</a>.
        Click dice signature next to Add dice to add dice to pool, press dice in pool to remove it.</p>
    </div>;
}